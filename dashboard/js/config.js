// TriFuel Dashboard — Configuration
// Fill in your Supabase project values, then open dashboard/index.html in a browser.
// Find these in: Supabase dashboard → Project Settings → API

window.TRIFUEL_CONFIG = {
  supabaseUrl:      "YOUR_SUPABASE_URL",      // e.g. https://xyzabc.supabase.co
  supabaseAnonKey:  "YOUR_SUPABASE_ANON_KEY", // public anon key (safe to expose)
  userId:           "YOUR_USER_UUID",         // the UUID from seed.sql (or your users table)
};
